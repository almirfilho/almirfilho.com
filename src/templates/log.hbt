{{> head }}

<section class="alm-main-content alm-main-content--with-padding">
    {{> content_header }}

    <ul class="alm-list">
        {{# each articles }}
            {{> list_item item=this lang=../lang schema=../schema }}
        {{/ each }}
    </ul>
</section>

{{> tail }}
