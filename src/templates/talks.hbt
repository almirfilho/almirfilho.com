{{> head }}

    <ul>
        {{# each talks }}
        <li>
            <h3>{{ this.title }}</h3>
            <img src="/images/talks/{{ this.slug }}.jpg">
        </li>
        {{/ each }}
    </ul>

{{> tail }}