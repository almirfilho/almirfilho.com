<li class="alm-list-item" {{> itemtype type=schema.type }}>
    <a class="alm-list-item-link"
        {{> itemprop prop=schema.url }}
        href="{{ item.url }}"
        target="_blank">
        <div class="alm-list-item-thumb">
            <img class="alm-avatar"
                {{> itemprop prop=schema.icon }}
                src="/images/avatars/{{ item.icon }}" />
        </div>
        <div class="alm-list-item-info" lang="{{ item.lang }}">
            <h3 class="alm-list-item-title" {{> itemprop prop=schema.title }}>
                {{ item.title }}
            </h3>
            {{# if item.date }}
                {{> date date=item.date itemprop=schema.date }}
            {{/ if }}
            {{# if item.desc }}
                <small class="alm-small">
                    {{# if item.type }}
                        <span class="alm-strong">{{ item.type }}</span>
                        <span class="alm-note" {{> itemprop prop=schema.desc }}>
                            {{ item.desc }}
                        </span>
                    {{ else }}
                        <span {{> itemprop prop=schema.desc }}>
                            {{ item.desc }}
                        </span>
                    {{/ if }}
                </small>
            {{/ if }}
        </div>
    </a>
    {{> schema_meta items=schema.meta }}
</li>
