<time class="alm-small alm-icon alm-icon-date"
    datetime="{{ date }}"
    {{# if itemprop }}itemprop="{{ itemprop }}"{{/ if }}>
    {{ dateformat lang date 'day' }}
    {{ dateformat lang date 'month' }}
    <span class="alm-strong">
        {{ dateformat lang date 'year' }}
    </span>
</time>
